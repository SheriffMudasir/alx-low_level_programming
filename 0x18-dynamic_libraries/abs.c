#include "main.h"

/**
 * _abs - This function computes the absolute value
 * of an integer
 * @n: the int to check
 * Return: 0 if successful
 */
int _abs(int n)
{
        if (n >= 0)
        {
                return (n);
        }
        return (-n);
}
