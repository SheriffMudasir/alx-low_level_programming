#include<stdio.h>
/**
 * main - This function use printf to print a string on the screen
 *
 * Return: 0 alway return if their are no errors
 */

int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
